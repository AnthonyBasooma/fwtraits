# For loading the API key from the environment.

For loading the API key from the environment.

## Usage

``` r
fw_keyload(keyvar = "API_KEY")
```

## Arguments

- keyvar:

  `string` Is the API name as indicated in the user environment. Default
  is `"API_KEY"`.

## Value

string of API
