# Function and instructions creating the API_KEY variable in the User environment.

Function and instructions creating the API_KEY variable in the User
environment.

## Usage

``` r
fw_setapikey(verbose = TRUE)
```

## Arguments

- verbose:

  `logical` If logical, then the instruction message will show. Default
  `TRUE`.

## Value

string of instructions for saving or setting the API in the R session.
